package com.nastena.pawsitive.dto;

public enum ErrorCode {
    UNKNOWN,
    UNAUTHORIZED,
    INVALID_REQUEST_BODY,

    LOGIN_CREDENTIALS_INVALID,

    REGISTER_CREDENTIALS_INVALID,
    USER_ALREADY_EXISTS

}
